# Repo-level files — not dotfiles to be deployed to $HOME
bootstrap.sh
install.sh
doctor.sh
Makefile
README.md
package.json
package-lock.json
.gitignore
ghostty/
docs/
tests/
scripts/
**/node_modules/**

# Deploy ~/.personal-zshrc on work machine, ~/.zshrc everywhere else
{{ if (index . "work") -}}
.zshrc
{{ else -}}
.personal-zshrc
{{ end -}}
